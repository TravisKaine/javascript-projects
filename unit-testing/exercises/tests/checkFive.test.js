const test = require('../checkFive');

describe("checkfive", function(){

    it("5 is 5", function() {
        let output = checkFive(2);
        expect(test(5)).toEqual("5 is equal to 5.");
        
    test("returns 'num is less than 5' when num < 5.", function() {
        expect(test(2)).toBe("2 is less than 5.");
         if (num > 5) {}

    test("returns num is more than 5' when num > 5.", function() {
        expect(test(8)).toBe("8 is more than 5.");
             if (num < 5) {}
 });
    })
    })
})